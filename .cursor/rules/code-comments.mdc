---
description: 生成或修改代码时必须补充功能注释
alwaysApply: true
---

# 功能注释

生成、修改代码时必须写功能注释，说明这段代码**做什么、为什么存在**，而不是把语句再翻译一遍。

## 要求

- 新模块 / 新文件：文件头用 1–3 行说明职责与在流水线中的位置。
- 新函数 / 类：用 docstring 或紧邻上一行注释写清输入、输出、副作用（读写存储、调模型、改状态）。
- 非显而易见的分支、策略路由、降级路径、正则/版面启发式：在分支处注明触发条件与业务意图。
- 注释用中文；与邻近已有注释语言保持一致。
- 禁止为每个赋值、import、getters 写注释。禁止写过时注释。

## 示例

```python
# ❌ 复述代码
# 遍历 sections 列表
for section in sections:
    text = section.get("text")

# ✅ 说明意图
# IEEE 章节正文：供 LLM 抽语义单元，参考文献章跳过以免重复清单条目
for section in sections:
    if str(section.get("id") or "").lower().startswith("reference"):
        continue
```

```python
def extract_paper_units(context, ...):
    """按论文结构产出 Knowledge Unit：元数据 + 章节 + 图/公式/表/引用清单，再按章做语义抽取。"""
```
